#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int num,fact=1;
	printf("Enter a number: ");
	fflush(stdout);
	scanf("%d",&num);
	for (int i = 1;i<=num;i++)
		fact*=i;
	printf("Sum = %d",fact);

}
