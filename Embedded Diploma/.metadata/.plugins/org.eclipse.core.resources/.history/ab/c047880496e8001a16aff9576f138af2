#include <stdio.h>
int main( )
{
    char CHAR;
    printf("Enter a char to check : ");
    fflush(stdout);
    scanf("%c",&CHAR);
    (CHAR % 2) ?  printf("%d is Odd",CHAR) : printf("%d is Even",CHAR);
    return 0;
}
