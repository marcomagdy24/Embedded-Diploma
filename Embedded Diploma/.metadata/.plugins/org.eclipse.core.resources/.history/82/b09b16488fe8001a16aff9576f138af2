#include <stdio.h>
int main(){
    char c;
    printf("Enter a character: ");
    fflush(stdout);
    scanf("%c",&c);        /* Takes a character from user */
    printf("ASCII value of %c = %d",c,c);
    return 0;
}
